public static int listLength(Node aNode){
        int length = 0;
        Node curr = aNode;
        while(curr != null){
            length++;
            curr = curr.next;
        }

        return length;
    }
    
